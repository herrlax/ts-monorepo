{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.8.0",
    "tslint": "^5.12.0",
    "tslint-react": "^3.6.0"
  },
  "scripts": {
    "lint": "lerna run lint",
    "build": "lerna run build",
    "release": "yarn lint && yarn build && lerna version"
  }
}
