{
  "private": true,
  "workspaces": [
    "apps/*",
    "modules/*"
  ],
  "devDependencies": {
    "lerna": "3.11.0",
    "tslint": "^5.12.0",
    "typescript": "3.3.3",
    "tslint-react": "^3.6.0",
    "@types/node": "^10.12.18",
    "@types/react": "^16.7.18"
  },
  "scripts": {
    "lint": "lerna run lint",
    "build": "lerna run build",
    "release": "yarn lint && yarn build && lerna version"
  }
}